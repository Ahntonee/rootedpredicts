# Category articles

Paste each article as HTML into its matching file below. Replace the file contents; do not add a full HTML page or scripts. These files are shared by the homepage category guides and the corresponding prediction page. Existing articles have already been moved here.

| Category | Article file | Page |
| --- | --- | --- |
| Free Pick | free.html | /predictions/free |
| Rooted Safe Tips | banker.html | /predictions/banker |
| 1.5 Goals | 1-5-goals.html | /predictions/1-5-goals |
| 2.5 Goals | 2-5-goals.html | /predictions/2-5-goals |
| 3.5 Goals | 3-5-goals.html | /predictions/3-5-goals |
| Acca Tips | acca.html | /predictions/acca |
| Away Win | away-win.html | /predictions/away-win |
| Both Teams To Score | btts.html | /predictions/btts |
| Double Chance | double-chance.html | /predictions/double-chance |
| Home Win | home-win.html | /predictions/home-win |

Admins can add, edit, format, or remove these articles under **Site Pages**. Saved articles are stored in `static_pages` with a `category-` slug prefix and override these default files on both the prediction page and homepage. An empty saved article means intentionally removed; it does not restore the default. Add content and save again to restore an article.

These files supply the initial content until an admin saves an override. Deploying a changed default file does not replace an existing admin override. The admin SEO Pages editor manages separate /tips/ articles.
